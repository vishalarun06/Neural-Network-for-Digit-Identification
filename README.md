# Neural-Network-for-Digit-Identification
This is an implementation of a simple neural network, from very first principles using only NumPy functions of matrix multiplication. It takes an input of a 28x28 pixel grid taken as 784 neurons and has two 16-neuron hidden layers. It finally then outputs the digit it recognises. I have also added a user interface to draw digits. 
